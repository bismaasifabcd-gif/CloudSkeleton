{
  "version": "v1",
  "hooks": [
    {
      "name": "AWS Credentials Check",
      "trigger": "PostFileSave",
      "description": "Verifies AWS credentials when working with infrastructure code",
      "matcher": "infrastructure|cdk|aws-.*|deploy",
      "action": {
        "type": "command",
        "command": "echo \"🔍 Checking AWS credentials and permissions...\" && aws sts get-caller-identity --output table 2>/dev/null || echo \"⚠️  AWS credentials not configured for deployment verification\""
      }
    }
  ]
}