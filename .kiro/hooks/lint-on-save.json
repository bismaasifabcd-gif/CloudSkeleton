{
  "version": "v1",
  "hooks": [
    {
      "name": "Lint on Save",
      "trigger": "PostFileSave",
      "description": "Runs linting and formatting checks after file saves",
      "matcher": "\\\\.(py|ts|tsx|js|jsx)$",
      "action": {
        "type": "command",
        "command": "echo \"Running post-save linting and formatting...\" && cd frontend && npm run lint --silent 2>/dev/null || echo \"Frontend linting skipped\" && cd ../backend && python -m black --check --quiet . 2>/dev/null || echo \"Backend formatting check skipped\""
      }
    }
  ]
}