{
  "name": "ai-data-pipeline-generator",
  "private": true,
  "version": "0.1.0",
  "description": "Full-stack AI data pipeline architecture generator using React, FastAPI, Amazon Bedrock, Lambda, API Gateway, S3, and DynamoDB.",
  "workspaces": [
    "frontend",
    "infrastructure"
  ],
  "scripts": {
    "dev": "npm --workspace frontend run dev",
    "build": "npm --workspace frontend run build && npm --workspace infrastructure run build",
    "test": "npm --workspace frontend run test && npm --workspace infrastructure run test",
    "cdk:synth": "npm --workspace infrastructure run cdk:synth",
    "deploy": "powershell -ExecutionPolicy Bypass -File scripts/deploy-all.ps1",
    "deploy:sh": "bash scripts/deploy-all.sh"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
